import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  text: {
    fontFamily: 'KFGQPC Uthman Taha Naskh Bold', // match the font's PostScript name
    fontSize: 18,
  },
});
